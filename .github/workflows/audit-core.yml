name: Audit Core Checks

on:
  push:
    branches:
      - main
      - feature/audit-core-foundation
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  audit-core:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Setup pnpm
        uses: pnpm/action-setup@a7487c7e89a18df4991f7f222e4898a00d66ddda # v4
        with:
          version: 10.17.1

      - name: Setup Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Typecheck audit core
        run: pnpm --filter @fortress/audit-core typecheck

      - name: Test audit core
        run: pnpm --filter @fortress/audit-core test
