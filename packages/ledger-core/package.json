{
  "name": "@fortress/ledger-core",
  "version": "0.1.0",
  "private": true,
  "description": "Pure-TypeScript double-entry ledger core for Fortress Fund",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --coverage",
    "test:watch": "vitest",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "typescript": "^6.0.3",
    "vitest": "^3.2.4"
  }
}
